package de.uni_hamburg.informatik.swt.se2.kino.werkzeuge;

import static org.junit.Assert.assertEquals;
import static org.junit.Assert.assertFalse;
import static org.junit.Assert.assertNull;
import static org.junit.Assert.assertTrue;
import static org.junit.Assert.fail;

import java.util.ArrayList;

import org.junit.Test;

import de.uni_hamburg.informatik.swt.se2.mediathek.fachwerte.Kundennummer;
import de.uni_hamburg.informatik.swt.se2.mediathek.materialien.Kunde;

public class BeobachtbarTest
{

    private Beobachtbar _drEvil
    private Beobachter _jamesBond;
    private Beobachter _sherlockHomes;
    private Beobachter _detektivConan;

    public BeobachtbarTest()
    {
        _drEvil = new Beobachtbar();
        _jamesBond = new Beobachter(_beobachtbar);
    }

    @Test
    public void testFuegeBeobachterHinzu()
    {
    }
    
    
}
